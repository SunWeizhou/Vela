import XCTest
@testable import Vela

final class HealthFoundationTests: XCTestCase {
    func testHealthKitAuthorizationTypes() {
        // Placeholder — HealthKit tests require device
        XCTAssertTrue(true)
    }
}
